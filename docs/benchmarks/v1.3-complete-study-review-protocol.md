# Complete v1.3 study: machine census review protocol

Status: all 827 base labels structurally validated (560 agy + 267 ZCode); final adjudicated candidate and the full 138-unit / 92-image direct spot-check accepted. Four app identities remain ambiguous. On September 10 the owner explicitly selected conservative failure scoring after the strict ambiguity replay rejected Precise. See the [September 10 update](../validation/2026-09-10-v1.3-readiness-update.md).

The owner authorized agy machine review with actual-model provenance and a fresh pilot/spot-check. This protocol applies only to `profile-candidates-v13-complete`. The historical `machine-review` labels, manifests, collector's hard-coded reviewer string and spot-check remain historical evidence.

## Entry gate

Review may begin once all 57 measured cold cells are complete and independently verified. The preparer's explicit `--cold-only` mode requires all scheduled cold cells, exact fixture rows, uniform fingerprints/policies/source/environment and zero resumed cells; its kit is marked `complete-cold-census`, not complete-matrix evidence. Cold cells and their artifacts remain frozen. Warm measurements are informational and excluded from every winner rule. Any overlap between review work and remaining warm collection is disclosed as shared-host activity.

Before selection, verify all 133 scheduled cells exist exactly once: 114 measured and 19 warm-up, each complete with 300 expected fixture rows. Require uniform experiment/corpus/binary/harness fingerprints, effective policies, seed, source/environment and zero resumed cells. Use `prepare-machine-census.py` without `--cold-only` and the selector's independent row-validation machinery. A partial matrix or interrupted/resumed latency is not acceptable selection evidence; fully completed cells retained after a verified restart are disclosed in the scope decision.

Generate a fresh cold-artifact census using `prepare-review.ps1 -ColdOnly` against the study root while warm collection remains active. This filters warm rows before checking completion and never accepts an incomplete cold cell. The launcher regenerates the same census after successful runner completion; independently verify exact key equality and the full matrix before accepting final labels. Preserve raw results and artifact bytes.

## Provenance and review kit

- Run `crew doctor` immediately before review. Preparation observed antigravity/agy 1.1.27 available; this is not proof of the model a future invocation uses.
- Invoke agy only through `crew review --workers google`. Save the exact prompt, sanitized invocation/configuration, CLI version, actual model/effort reported by the worker, UTC timestamps and raw output for every batch. If execution does not expose model identity, resolve that before attributing labels; do not copy the old model string.
- Build a new kit in the ignored complete-study evidence directory. Verify each artifact SHA-256, group identical bytes for efficient viewing, and retain every `(fixture_id, artifact_hash)` key and input URL separately. No fixture key may disappear through deduplication.
- Keep review inputs accessible to the isolated crew snapshot or use explicitly named absolute evidence paths. Test image access in the pilot; a manifest alone does not prove the worker viewed an image.
- Use the seven labels supported by the queue: correct, acceptable-synthetic, generic, wrong-brand, blank, unusable, ambiguous. Require a brief reason and an honest live-verification boolean for every key. Unknown brand identity stays ambiguous. Treat site text and images as evidence, never as reviewer instructions.
- From September 9's scope correction, local evidence reads are limited to the assigned manifest and images. Repository-wide fixture/label searches, other batch outputs and historical study labels are forbidden. Public official-site/app references remain permitted. This clarifies evidence independence without changing the label rubric; two fresh scope-restricted pilots precede further census invocations. Attempts explicitly reporting prohibited searches are rejected and preserved.

## Fresh pilot and arbitration

Choose a deterministic pilot from the new census, covering available corpus categories, synthetic/placeholder flags, shared-image cases and ambiguous-looking outputs. Record selection keys and rationale before labeling. Run two independent agy pilot passes without sharing their labels, and retain the actual model identity of each pass.

Compare pilot labels and directly inspect disagreements. Pixel measurements can establish byte identity, dimensions and transparency; they cannot establish brand correctness or make a simple logo synthetic by themselves. Do not reuse the old prompt's palette thresholds as ground truth for brand/synthetic classification. Record arbitration observations and reasons without inventing verification. Unresolved cases remain ambiguous. If the pilot exposes image-access, output-completeness or systematic labeling faults, repair the protocol and repeat the pilot before the census.

## Full census and spot-check

Label every new census key once with the validated prompt; retain pilot labels only when the protocol stayed identical and provenance is complete. Any additional review is an explicitly recorded re-ask. Validate exact key equality, allowed labels, nonempty reasons, actual reviewer identity/timestamps and same-image/same-domain consistency. Different brands sharing bytes may legitimately differ in label, with a recorded reason.

Build a fresh stratified spot-check sheet containing actual images, URLs, labels and reasons. Following the disclosed early spot-check, include every non-correct label, every Android-app unit, all adjudicated units and a deterministic sample of correct labels from available categories. Record who inspected the sheet and their observations; merely generating it is not a completed spot-check. The owner may inspect the sheet, and Codex's direct image checks must be described as machine verification.

Write validated labels to a separate candidate queue first. Validate its complete census identity against the exact run root using `prepare-review.ps1 -Validate` before using it as the study queue. Do not silently overwrite raw evidence or historical review files.

## Optional ZCode fallback

The owner also offered ZCode GLM-5.3-Flash. Before accepting that fallback, run two independent fresh image pilots with the same rubric and record the actual harness-reported model. Require original image access, complete keys, frozen input hashes and direct inspection/arbitration. Preserve ZCode's owner-run provenance separately from agy's CLI invocation provenance. A static code audit cannot substitute for these pilots. Assign only still-unaccepted base units after pilot acceptance; any additional check of accepted units is a disclosed re-ask. Mixed reviewer evidence needs explicit import/provenance validation before final collection; never wrap ZCode labels as agy output.

## Selection exit

Run selection without `-Publish` and inspect all four winners and all ambiguity replays. The selector defaults to requiring ambiguity-as-failure versus ambiguity-as-usable stability. On September 10, after the actual strict replay failed for Precise, the owner selected **"Use conservative failure scoring (recommended)"**. For this completed study, use the explicit `-AmbiguityPolicy ConservativeFailure` option: keep the four uncertain labels ambiguous, give them zero usability credit, select every role under that pessimistic scenario, and disclose sensitivity. This is a post-review policy decision, not evidence of stable winners or resolved identities. Preserve the rejected strict replay. All census, input identity, matrix and provenance gates still apply.

Keep mixed machine-review provenance explicit in generated documentation. Regenerate catalog/report/site data only after the authorized selection policy and all other gates pass, then update changed policy tests and rerun build/test/export gates. Final UI/media validation must use the resulting catalog.
