# Expanded v1.3 study — machine review record

Status as of September 10: final 827-unit adjudicated candidate validated; full 138-unit / 92-image direct spot-check accepted. Base provenance remains 560 agy + 267 ZCode; reasks 01–05 and direct Codex corrections are disclosed. Four app identities remain ambiguous. After strict replay rejected Precise, the owner explicitly selected conservative failure scoring, retaining uncertainty and reporting all replay results. See the [current selection record](../validation/2026-09-10-v1.3-selection-and-freeze.md). Earlier counts and pending statements below are historical checkpoints.

The owner authorized agy review with actual-model provenance and a fresh pilot/spot-check. The complete cold census was independently verified before review: 57 complete non-resumed cells, 17,100 fixture rows, 827 unique fixture/hash units and 624 image hashes. The immutable review kit is `artifacts/v1.3-preparation/complete-study-review-20260908` (ignored). Its manifest records source, schedule, environment and review-tool hashes. Historical labels were not imported.

## Pilot

Two independent `crew review --workers google` invocations reviewed the same deterministic eight-image, 20-unit pilot. Both used agy 1.1.27, with actual command arguments `--model gemini-3.8-flash-high --effort high`. Both returned successfully with complete, valid labels and unchanged manifest/prompt/image inputs. All 20 labels agreed (`correct`); no disagreements required arbitration. Raw invocations, timestamps with offsets, input hashes, reasons and comparison are retained in `pilot-a`, `pilot-b` and `pilot-comparison.json`.

Codex directly viewed all eight local images and compared their visible features with both pilots' reasons: Stack Overflow bars, the WIRED W, Pi-hole's red swirl and leaves, Spiegel's geometric S, Outlook's O/envelope, Notion's N cube, GitHub's Octocat, and Indie Hackers' IH mark. No material conflict was found. The Pi-hole and IH images demonstrate that synthetic/blank flags are not authoritative labels. A tentative uncertainty about the small WIRED mark was resolved by viewing the [favicon declared by WIRED's live page](https://www.wired.com/verso/static/wired-us/assets/favicon.ico). Codex's observations are machine inspection, not human verification. Pilot agreement alone is not an accuracy estimate.

The original prompt covered the initial accepted batches. September 9's scope correction introduced an explicitly recorded second template, described below. Full batches label every census key, including pilot keys, so these pilot units receive additional disclosed review passes. The final queue will contain one accepted label per key; it does not imply one review pass.

## Full census and final gates

The initial plan used twenty-six isolated batches of at most 24 images through the same driver and collector. Batch 07 exceeded the model output limit and returned an error; its raw output was rejected and archived, and its unchanged inputs were split into disjoint 12-image batches 07a/07b. There are now 27 required batches. Every batch must retain actual invocation model/effort, timestamps, input identity and a complete label array. Independent batches may run concurrently. Any failure or incomplete output is rejected and recorded before a new invocation.

Pending: complete batch validation, same-image/domain consistency, fresh stratified direct spot-check, any required re-asks, exact final queue equality and stable ambiguity replay. All 133 measured/warm-up cells passed independent validation. Review overlapped informational warm collection on the shared host; scoring uses already-finished cold measurements only. See the [scope decision](v1.3-study-scope-decision.md) and [review protocol](v1.3-complete-study-review-protocol.md).

## Early spot-check and disclosed re-ask

Direct inspection found that batch 03 incorrectly called Stratechery's genuine pen/circuit mark synthetic. Codex viewed the [favicon declared by Stratechery's official page](https://i0.wp.com/stratechery.com/wp-content/uploads/2018/03/cropped-android-chrome-512x512-1.png?fit=192%2C192&ssl=1) and confirmed the visual match. Batch 04 also accepted the same gray W placeholder for `www.shop.ca` and `www.aaronsw.com`; that common prefix does not identify either site.

A separate three-unit `reask-01` used the same rubric plus these concrete concerns and the official reference. It returned `correct` for Stratechery and `generic` for both W units. Codex accepted those results for a separate adjudicated candidate queue after direct inspection and normal batch validation. Original labels, the unchanged main prompt and the focused context remain available. This is an additional disclosed review pass, not a silent relabeling. The final spot-check is expanded to every non-correct label and every Android-app unit, plus stratified correct examples and all adjudicated units.

Batch 12 and reask-01 exposed an agy transport quirk: progress lines were concatenated before the final JSON array. The collector now accepts only a narrow Checking/Downloading/Waiting preamble with no failure terms or earlier structured response, followed by an exact JSON array and no trailing prose. Four new regressions cover this boundary (14 collector tests pass). Raw responses remain intact; label, image, provenance and complete-key checks are unchanged. The collector hash recorded at initial kit creation predates this transport-only parsing extension; final adjudication records its tool/input identities separately.

Collection completed at 2026-09-08T10:01:57Z including final queue generation. The independent default-mode preparer passed all 133 cells and exact 827-key census (`artifacts/v1.3-preparation/complete-matrix-validation.log`). No cell was resumed. Final review and selection remain pending.

## Quota interruption and safe resumption

At approximately 10:27–10:28Z, six agy invocations failed with `Individual quota reached`; the provider reported a reset approximately 14:37Z (16:37 Europe/Rome). These are provider quota errors, not sandbox approval failures. Eleven batches validated 338 of 827 units. Batches 07a, 07b, 11, 14, 16 and 17 produced no accepted labels; their complete attempts are retained in `failed/<batch>-quota-attempt-1`. Their restored manifests and images match every original execution hash. Ten other batches have not started. No retry is currently running and no automatic wakeup is scheduled.

The ignored `artifacts/v1.3-preparation/resume-census.ps1` validates and skips completed batches, runs remaining batches sequentially, and stops on the first failure. It preserves existing output and requires failed attempts to be explicitly archived before another retry. After resumption, full collection, accepted re-ask application, direct spot-check and ambiguity replay are still required. The candidate adjudicator now requires and hashes each recorded acceptance, retains original labels, and records its current collector hash. Six adjudicator boundary tests and fourteen collector tests pass.

## September 9 continuation

The owner explicitly requested completion with agy Gemini 3.8 Flash. Three disjoint review lanes resumed the 16 remaining batches. Batch 07b completed with 18 validated labels, bringing the base census to 356/827. The three terminal-backed lanes then disappeared; a process inventory confirmed they were absent. Unfinished attempts for 07a/11/15 were preserved under `failed/<batch>-interrupted-20260909` with zero accepted labels, and their exact manifest/images were restored. New hidden processes use persistent per-lane logs and `artifacts/v1.3-preparation/resume-census-20260909.py`; no experiment cell was rerun.

The installed agy CLI began reporting 1.1.28 after earlier invocations reported 1.1.27. Actual model/effort remain `gemini-3.8-flash-high` / `high`, and each invocation retains its actual CLI version. Codex did not reconfigure or reinstall the worker. The lane wrapper accepts explicitly recorded CLI-version changes while still rejecting a different model/effort.

Codex directly inspected 41 images covering 52 units from completed batches; `direct-observations-01.json` records concrete visible features. This is partial machine inspection, not final census acceptance. An additional official-asset comparison confirmed the sampled Joel on Software favicon. The final spot-check will reconcile these observations against the complete adjudicated queue and inspect every remaining selected image.

`reask-02` resolved four genuine-logo/synthetic disputes: Takealot's white t on blue, two Kakao black-k/yellow sizes, and eMAG's red-purple e. Codex viewed both original images and each site's declared official favicon; agy independently returned `correct` for all four. `reask-03` returned `unusable` for a heavily blurred Spotify image previously labeled correct, matching Codex's direct observation that distinct soundwaves were lost. Both re-asks used agy 1.1.28 / Gemini 3.8 Flash high effort, passed ordinary batch validation, and have explicit acceptance records. Together with reask-01, eight changed units are accepted for later separate adjudication; no original label output was overwritten.

Batch 07a later validated 14 units (base census 370/827). Completed attempts for batches 11 and 15 were rejected: their response preambles explicitly reported searching existing labeling patterns across the repository or searching other preparation artifacts by fixture ID. This creates a risk of prior-label exposure, regardless of otherwise complete output. Both raw attempts are retained under `failed/<batch>-evidence-scope-20260909`; zero labels from them are accepted. A scan of the other completed response preambles found no similar admission; this is limited observable evidence, not proof of every hidden worker action.

The main prompt now explicitly restricts local reads to the assigned manifest/images, forbids repository/other-batch/historical-label searches, and permits public official references. The rubric is unchanged. Two fresh `pilot-scope-a`/`pilot-scope-b` invocations use byte-identical original pilot images and the restricted prompt before additional census work. Every invocation preserves its own prompt hash so earlier and revised instructions remain distinguishable.

Both restricted pilots completed with agy 1.1.28 / `gemini-3.8-flash-high` / high effort. They agreed on 19/20 units. The disagreement called Indie Hackers' IH glyph generated in one pass; Codex directly compared it with the official page-declared [IH logo](https://storage.googleapis.com/indie-hackers.appspot.com/branding/logos/indie-hackers-logo__glyph--light.svg) and retained `correct`. The other visible-feature reasons matched the eight previously viewed, byte-identical pilot images. `pilot-scope-comparison.json` and `pilot-scope-acceptance.json` preserve the comparison and arbitration. The accepted restricted template contains a duplicated scope paragraph; its exact bytes are retained, not silently cleaned up mid-census.

`census-prompt-policy.json` maps every required batch name to either the original or restricted template, with separate accepted-pilot records. The driver and collector enforce that mapping; unrecorded prompt drift fails. Initial kit tooling hashes are not overwritten. [ZCode review resolutions](../validation/2026-09-09-zcode-review-resolution.md) document subsequent evidence-tool hardening, including Google Play accounting, zero-resumed validation, overwrite refusal and exact transparency checks.

After batches 14/15 completed, a second direct observation record added ten images and 36 units. A MetaFilter MF mark was disputed as incorrectly synthetic: Codex viewed the original and the page-declared same-site [touch icon](https://www.metafilter.com/apple-touch-icon.png), observing matching italic yellow-green M and white F on blue. The CDN favicon reference was not accessed after two automatic approval timeouts. `reask-04` independently inspected the original and returned `correct`, honestly recording `verified_live=false`; Codex performed the official-reference comparison and accepted the correction. All other newly inspected labels matched their visible evidence. Nine corrections across reasks 01–04 are accepted for later adjudication.

The next full-size batches 17/18 ended with generic worker `ERROR`, nonzero exit status and empty response. No label was accepted and no precise cause is claimed. Their attempts remain under `failed/<batch>-worker-error-20260909`; exact inputs were partitioned into disjoint twelve-image batches 17a/17b and 18a/18b. The required set is now 29 batches with the same 827 keys and 624 image hashes. The prior prompt policy is preserved and the replacement mapping records its SHA-256. The rubric and accepted templates did not change.

## September 9 afternoon checkpoint

Batch 11 subsequently returned a stream `ERROR` despite the outer crew command exiting zero; that response was rejected in full. Its inputs and the unstarted 16/19–26 batches were split into disjoint twelve-image assignments. Batch 17b also returned a stream `ERROR` and was rejected, then split into six-image 17b1/17b2. Original failed responses and unstarted superseded inputs remain in `failed` and `superseded`, respectively. Each policy amendment retains the preceding policy and records its hash. The current 40 assignments still contain exactly 827 unique keys and 624 unique image hashes; neither the rubric nor the accepted templates changed.

Successful 17a/18a/18b/11a/11b/16a outputs brought the base count to 560. Batch 17a exposed two literal agy transport lines about launching and waiting for a live verification request. The parser now recognizes only those exact line forms in addition to its earlier progress forms, and rejects error or evidence-scope admissions. The raw response is unchanged. Eighteen collector regressions pass; label and provenance requirements are unchanged.

Five subsequent invocations (16b/19a/19b/17b1/17b2) returned provider quota errors. Their reported reset durations converge on approximately 18:51 UTC / 20:51 Europe/Rome on September 9. All five attempts remain under `failed/<batch>-quota-20260909`; exact manifest/image inputs were restored and no labels were accepted from them. Every worker has stopped. The remaining 267 units occupy 19 batches. `artifacts/v1.3-preparation/resume-census-current.py` replaces the obsolete lane assignments: its default mode verifies and prints the current plan, while explicit `--run` waits for no timer and refuses execution before 18:52 UTC. It validates and skips accepted batches, invokes remaining batches sequentially through the ordinary crew driver, and stops on any failure. No automatic wakeup is scheduled.

Direct observation records 01–04 now cover 60 images and 103 unique units. Two orange Amazon smile icons for the Shopping package are unresolved, including a reconsidered observation from record 01. Codex directly viewed both originals and the icon in the [official Shopping listing](https://play.google.com/store/apps/details?id=com.amazon.mShop.android.shopping), which currently depicts a tan box with blue tape and a black smile. A different current icon does not establish that a historical design is wrong. The focused two-unit `reask-05` is prepared to investigate exact app identity, with `ambiguous` required if it cannot be established. It has not run; neither original label has changed. The nine accepted corrections in reasks 01–04 also remain unapplied until full base collection.

The owner's offered ZCode GLM-5.3-Flash fallback is prepared separately in `artifacts/v1.3-preparation/zcode-census-recovery-20260909`. It copies only the 267 unaccepted units plus two independent, unlabeled copies of the eight-image pilot. The owner has been given the two pilot prompts; no results have arrived. Census work requires direct pilot acceptance first. Actual model identity must be reported from the ZCode harness, with this owner-run provenance distinguished from agy's recorded invocation arguments. No mixed-review import or pilot acceptance is claimed. The five verified static audit findings and additional disclosure correction are documented in the [resolution record](../validation/2026-09-09-zcode-review-resolution.md).

## ZCode pilot acceptance and remaining assignments

The owner returned both pilots. Pilot A reports 16:00:25–16:04:18 UTC and pilot B 16:01:31–16:09:20 UTC, with model ID `builtin:zai-coding-plan/GLM-5.3-Flash` taken from each session's harness system-prompt declaration. This is owner-run, harness-reported provenance; Codex does not independently attest the underlying service model. The owner reports separate fresh conversations. Both `transcript.txt` files explicitly identify themselves as agent reconstructions, not raw harness exports. They also describe temporary reference downloads, so claims that absolutely no writes occurred outside the assigned directories are too broad. The actual review inputs remain byte-identical.

Both pilots pass exact hash, timestamp, image-inspection field and 20-key validation. Their images and unit metadata equal the original prepared pilot; labels agree 20/20 (`correct`). Codex directly viewed all eight originals again: Stack Overflow, WIRED, Pi-hole, SPIEGEL, Outlook, Notion, GitHub and Indie Hackers. Two reasons contain foreground/background description errors: pilot A describes IH as white on navy, whereas the original is black on white; pilot B describes a white Octocat silhouette, whereas the original has a dark silhouette in a white disc. Neither changes brand identity or the accepted label. Original outputs remain untouched; `pilot-comparison.json` and `pilot-acceptance.json` record the observations, provenance limitations and exact evidence hashes. Agreement on this pilot is not an accuracy estimate.

Acceptance was recorded at 16:26:50 UTC. `RUN-CENSUS.md` supplies one fresh-conversation instruction per remaining batch. Each generated prompt preserves the accepted restricted rubric, records the actual reported model and binds its own SHA-256 in the returned evidence. The separate `census-assignment-policy.json` fixes all 19 prompts and original input hashes. No earlier labels are copied into these assignments. Census results remain pending; the two disputed Shopping icons remain a separate focused re-ask.

The collector now accepts an explicit `--zcode-kit` alongside the original `--kit`. `zcode_review_source.py` verifies accepted pilot evidence, prompt and input identity, original parent manifests/images, assignment timestamps and transcripts; it rejects dual agy/ZCode evidence for the same assignment. ZCode labels receive `machine:zcode-owner-reported/...` provenance, with no invented crew run ID or effort. The final validation record identifies every source path and hash. Ten new boundary/integration tests pass, including a two-source synthetic census; all 50 evidence regressions and release-workflow checks pass. No real combined queue or final selection has been generated.

## September 10: census independently validated

All 19 ZCode outputs pass strict validation after evidence-only parsing support for delivered hash layouts and annotated timestamps; originals remain untouched. The mixed-source collector and separate queue validator pass 827 units; all 133 cells reverify. All 57 evidence tests pass. Nine accepted reasks 01–04 are reflected in `review-queue-adjudicated-01.csv`.

Codex viewed 21 originals covering 26 selected units, including all 17 non-correct ZCode units and category examples. Seven labels require disclosed correction: three badssl icons are genuine site assets, Zoom uses the zm favicon, and three Paul Graham artifacts match its favicon or declared Arc icon. Two comparisons are pixel-identical. NU synthetic attribution, Office/Copilot and specific Amazon Shopping identity require focused review; original reask-05 remains untouched. The expanded candidate sheet selects 138 units before reconciliation with prior observations; this is not final spot-check acceptance. Full observations and reference hashes are in `artifacts/v1.3-preparation/audit-20260910/final-validation.json`. No final selection, production binary change or publication occurred. Do not resume obsolete base-census assignments.
