# v1.3 provider-study scope decision

On 2026-09-07 the owner selected **"Run the expanded study"** when offered retaining the old study with a disclosed omission versus running the expanded study. This selects Route A in the release-completion plan; there is no waiver of the missing comparison.

Run `profile-candidates-v13-complete`: all 18 original candidates unchanged plus `cand-full-minus-yandex-thorough-synth`. The new candidate uses Direct Site, Twenty Icons, DuckDuckGo, Google, Favicone and Icon Horse, with 10000/5000/45000 ms budgets, synthetic fallbacks enabled, early stop disabled and Android store lookup enabled.

The experiment uses the same 300 public fixtures, three repetitions, concurrency eight, cold/warm modes and seed 20260827. Expected output is 114 measured cells and 19 unmeasured warm-up cells, with 300 rows per cell. Its separate output root is `eng/benchmark-runs/profile-candidates-v13-complete`. Historical evidence and generated winners remain unchanged until the new census is reviewed and selection passes.

## Execution constraints

- Freeze the experiment, binary, corpus and execution-harness hashes before launch and compare them with `schedule.json` and every completed cell.
- Start with an absent output root, on one machine using Windows PowerShell 5.1. Do not resume interrupted cells or reuse partial-cell evidence. Fully completed cells may be retained only after verifying schedule, fingerprints, source, environment, complete fixture rows and non-resume metadata. Quarantine an interrupted cold cell and rerun it from zero; an interrupted warm block requires a fresh warm-up and all its measured repetitions. No resumed latency can influence selection.
- The resume accumulator, quarantine ordering and cross-machine uniformity follow-ups remain unresolved in the historical harness. This execution deliberately excludes resume and cross-machine evidence; these constraints must be checked before selection. The fingerprinted runner/module remain unchanged.
- Do not rebuild or change production code, runner/module, corpus or experiment while this study runs. Store its source SHA and environment with the launch record.
- Generate the entire cold-artifact census after all 57 cold cells complete. Review may overlap the remaining informational warm runs, with that shared-host activity disclosed; all 133 cells remain mandatory before selection. The owner explicitly selected **"Machine review with agy, recording its actual model and a fresh pilot/spot-check"** for this new census on 2026-09-07. Record actual model/reviewer provenance before labeling; never transplant old labels or call machine judgments human review. Follow [the new review protocol](v1.3-complete-study-review-protocol.md).
- Select without publication first; require complete row/fingerprint/environment checks. The original requirement was ambiguity replay stability. On September 10 the owner explicitly approved conservative failure scoring after the strict replay rejected Precise; retain all replay results and report this policy change as described in the [review protocol](v1.3-complete-study-review-protocol.md). Only then regenerate the catalog/report/site export and rerun affected gates.

## Verification

`test-complete-study.ps1` rejects the original 18-candidate definition, accepts exactly 19 ordered unique IDs, verifies all old definitions are unchanged, checks the full-minus-Yandex chain and budgets, resolves all policies using the production constructor/catalog, checks its fingerprint is distinct and verifies removing the candidate changes experiment identity. The full harness self-test invokes this contract.

This decision authorizes the expanded run. It does not declare the old or new study release-ready, waive host/site gates, or authorize tagging/publication.

## Frozen launch inputs

Production source: `18d49093d10e0dd819eea8a21e52cad4cc91f6ac`; test/experiment source is the commit containing this decision. KeePass 2.60.0.0, .NET SDK 9.0.317; launch uses Windows PowerShell 5.1.

- binary_sha256: `415F66BC2190D4FFB71E48FACC08763C6D0F37DCCDFC3B6788A8176194FC846D`
- experiment_sha256: `72E008DA8CAE7A2AD9F4612AEA7E96ED4B01E68DF2BF0F1A7F08E5E8B45E8F9D`
- corpus_sha256: `6479056D7A05AC0B138E2660D640A1AA4293EF2919081027BE4BD7083AC2FA02`
- execution_harness_sha256: `1be50e6e5b2a973dca5bbd7bb5bffcdc730e3b379aa22e2991c7fae30f7b6c23`

The clean study launched at 2026-09-07T23:21:11.2159584Z (2026-09-08 local time), from `cac2cb07361a99626dd3463697907e1ac84f0805`, Windows PowerShell 5.1.26100.9168, Windows 10.0.26200, on ZENTIE. Its 133-cell schedule and first cell independently match all four frozen hashes. Process ID at launch: 32640. The first launch attempt failed before any cell or output root existed because the child process inherited an incompatible PowerShell module path; the successful launcher restricts module lookup to Windows PowerShell's own modules. No benchmark source changed for that repair.

The ignored launch record and console logs under `artifacts/v1.3-preparation` record actual progress. Collection ultimately completed at 2026-09-08T10:01:57Z after the recoveries below; independent final validation passed all 133 cells and the exact 827-unit census. Full machine review and profile selection remain pending; see the [review record](v1.3-complete-study-review-record.md) for the quota interruption and resumption procedure.

### Cold-cell interruption and recovery

On 2026-09-08 the first process was found absent after its last progress at 2026-09-07T23:54:57Z (250/300 sites in cell 27). It had not recorded an exception or completion; the cause is unconfirmed. The incomplete `run_20260908_015412_c759a2ec` was moved into `eng/benchmark-runs/quarantine/complete-study-interrupted-20260908` after verifying the schedule identity. The 26 completed cells had matching source/environment/fingerprints, 300 recorded rows and `resumed=false`.

Execution restarted in a retained terminal session at 2026-09-08T00:06:16Z, PID 42740, using the identical source, binary and Windows PowerShell environment. The incomplete cell runs from zero; completed cells are skipped. Final validation additionally checks every row and the entire matrix before accepting retained evidence. This recovery changes the earlier blanket restart instruction to preserve independently valid complete cells, while keeping the no-resumed-latency rule. No fingerprinted runner/module change was made. Logs: `artifacts/v1.3-preparation/study-console-continued.log`.

### Warm-block interruption and recovery

At 2026-09-08 10:57 local time, process 42740 was found absent while its status file still said running. Its final recorded row was at 01:25:28Z in `cand-direct-google-balanced` warm-up `run_20260908_032507_0886e65a`; no exception or completion was recorded and the cause is unconfirmed. All 69 completed cells (57 cold and three complete four-cell warm blocks) passed fresh schedule/source/environment/fingerprint checks, exact fixture and row identity checks for 20,700 rows, and SHA-256 checks of their referenced artifacts. No resumed cells were accepted.

The interrupted warm-up was moved outside the run root into `eng/benchmark-runs/quarantine/complete-study-warm-interrupted-20260908`. No measured repetition from that block existed. The unchanged runner restarted at 09:02:20Z, PID 30688, retained the 69 verified cells, and began a fresh warm-up followed by all three measured repetitions. Log: `artifacts/v1.3-preparation/study-console-recovery-2.log`. The complete matrix remains required before census review. Collection spans interruptions on a shared developer machine; bounded local website checks also ran on that host, so this is not an isolated performance laboratory.
