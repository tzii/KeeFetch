## Handoff — 2026-08-17 (updated same day, post study launch)

Active branch: `codex/v1-3-profile-selection` at `20a419b` (pushed; PR #4 tracks it)

Draft PR: [#4 — Scaffold v1.3 profile selection tooling (Task 5a)](https://github.com/tzii/KeeFetch/pull/4)

Base: `master` at `13fd9b8`

### What changed in this session

Machine context: this session runs on a different workstation (`C:\Dev\KeeFetch`,
user `zxcv0`) than the morning session (`C:\Users\simon\Documents\Projects\KeeFetch`).
Environment was rebuilt from scratch:

- Portable KeePass 2.60 extracted to `C:\Dev\tools\KeePass-2.60` (matches the CI
  choco pin). Builds pass `-p:KeePassPath`; the benchmark runner honors the
  `KEEFETCH_KEEPASS_PATH` environment variable.
- No .NET Framework targeting pack is required; the SDK auto-restores reference
  assemblies.

Four commits landed on the branch (all pushed, CI green):

1. `01eb284` — `eng/benchmark-presets.ps1` was unrunnable end-to-end (three
   latent defects, invisible to CI because CI only exercises the module
   self-tests): the `$experiment` variable collided with the typed `[string]
   $Experiment` parameter and was silently stringified; `Load-CandidateMap` was
   called before its definition; the KeePass path is now env-overridable.
2. `c2b4160` — Plan 02 Task 6 complete: `eng/export-profile-data.ps1`
   (deterministic UTF-8-no-BOM export of visible managed profiles with a
   `-Check` drift mode), `site/data/profiles.json`,
   `KeeFetch.Tests/ProfileExportTests.cs` (2 new tests, suite now 118/118),
   and a `-Check` gate in `build.yml` after the Release build.
3. `d6c4557` — `build.yml` moved off deprecated Node 20 actions:
   checkout v6, setup-dotnet v6, upload-artifact v6, gh-release v3.
   (`pages.yml` was already on v5/v6.)
4. `20a419b` — fixed an indefinite-hang bug observed live against
   www.tiktok.com during the study: .NET Framework response streams ignore the
   cancellation token on `ReadAsync`, so a server that sends headers and then
   stalls blocked the fetch forever. All four read sites now register disposal
   of the `HttpResponseMessage` on the deadline CTS, which aborts the socket.
   Live tiktok fetches now complete in ~1.2 s.

### Study state

Task 5b's live study is **running** (background, checkpoint/resume capable):

```powershell
$env:KEEFETCH_KEEPASS_PATH = "C:\Dev\tools\KeePass-2.60\KeePass.exe"
powershell -NoProfile -ExecutionPolicy Bypass -File eng/benchmark-presets.ps1 `
  -Experiment eng/benchmark/experiments/profile-candidates-v13.json
```

- Output: `eng/benchmark-runs/profile-candidates-v13/` (gitignored),
  console log at `eng/benchmark-runs/profile-candidates-v13-console.log`.
- 18 candidates x 2 cache modes x 3 repetitions x 300 rows (~32.4k fetches),
  concurrency 8. Observed pace implies roughly 6-9 hours total.
- The one aborted attempt (run recorded under the pre-fix binary) was deleted;
  all data in the output root comes from `20a419b` (recorded in each `run.json`).
- If the process dies mid-run: resume the incomplete run with
  `-ResumeRun <run-dir>` (completes that single run), then relaunch without
  `-ResumeRun` for the remaining runs, or just delete the incomplete run dir
  and restart; the harness checkpoints per run, not per experiment.

### Resume here (once the study completes)

1. Generate the review queue: `./eng/benchmark/prepare-review.ps1` (run from
   repo root with the run root as `-RunDir eng/benchmark-runs/profile-candidates-v13`).
   Do not commit raw run artifacts or queues containing local paths.
2. **Human labeling** (the actual gate; cannot be automated): labels are
   `correct`, `acceptable-synthetic`, `generic`, `wrong-brand`, `blank`,
   `unusable`, or `ambiguous`; every row must carry reviewer + UTC timestamp.
   `./eng/benchmark/prepare-review.ps1 -Validate` checks the filled queue.
3. Select winners: `./eng/benchmark/select-profiles.ps1` (rejects any
   `not-reviewed` row). Review and commit `FetchProfileCatalog.Generated.cs`
   and `docs/benchmarks/v1.3-provider-study.md`.
4. Re-run `eng/export-profile-data.ps1` and commit the regenerated
   `site/data/profiles.json` so the website data matches the new catalog
   (the Task 6 CI gate enforces this).
5. Continue with Plan 03 (guided-native UX), Plan 04 (website), Plan 05
   (release validation). Do not start Plan 03 until the catalog is frozen.

### Fresh verification (this session)

- Release build + `-p:LangVersion=5 -warnaserror`: 0 warnings, 0 errors.
- Full test suite: 118/118 passed (116 prior + 2 new export tests).
- `eng/export-profile-data.ps1 -Check`: passes.
- Harness self-tests: passed. Live smoke experiment: passed.
- GitHub Actions green on `20a419b` (build, C# 5, tests + 118, harness,
  profile-data consistency gate, PLGX, artifacts).
- Live tiktok.com probe: bounded ~1.2 s, status Success, 4/4 runs.

### Required gates before advancing PR #4

- The study completes; review queue fully labeled by a human.
- Selection outputs reviewed; catalog + evidence report committed.
- `eng/export-profile-data.ps1 -Check` passes after regeneration.
- C# 5 Release build warnings-as-errors and full tests stay green.
