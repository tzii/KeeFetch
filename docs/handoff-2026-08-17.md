## Handoff — 2026-08-17

Active branch: `codex/v1-3-profile-selection`

Draft PR: [#4 — Scaffold v1.3 profile selection tooling (Task 5a)](https://github.com/tzii/KeeFetch/pull/4)

Base: `master` at `13fd9b8`

### Publication audit

The repository was fetched with pruning and tags before this audit.

| Local branch | Tip before this handoff | GitHub state |
|---|---:|---|
| `master` | `13fd9b8` | Matches `origin/master` |
| `codex/v1-3-profile-catalog-foundation` | `62059ab` | Matches its upstream and is merged into `master` |
| `codex/v1-3-profile-selection` | `8366242` | Matched its upstream before this handoff commit; PR #4 now tracks it |
| `feature/availability-first-reliability-push-20260412-004334` | `f67d802` | Matches its upstream and is merged into `master` |
| `worktree-codex+v1-3-benchmark-foundation` | `d459778` | No upstream branch, but the tip is already contained in `origin/master` |

- All four local release tags (`v1.0.0` through `v1.2.0`) match GitHub.
- No stash entries exist, and there are no linked Git worktrees beyond the primary checkout.
- The remote-only historical branch `origin/fix/pre-public-cleanup` remains available on GitHub.
- Local Cline checkpoint refs contain only snapshots of the generated files described below; they do not contain unpublished KeeFetch source changes and are not publication branches.

The following local material is intentionally excluded from KeeFetch history:

- `.commandcode/`: local agent preference memory.
- `.superpowers/`: generated design previews plus local preview-server logs, PIDs, ports, and click state. The durable product direction is already represented by the checked-in v1.3 spec and plans.
- `better-harness/`: an independent clone of `QoderAI/better-harness`, not a KeeFetch submodule. Its `main` checkout was clean and matched `origin/main` at `9fd12f2` during this audit.

These paths are now ignored so they remain available locally without making the KeeFetch worktree appear dirty.

### Current implementation state

Task 5a is complete on this branch:

- `profile-candidates-v13.json` defines 18 explicit custom candidates.
- `prepare-review.ps1` builds and validates the human review queue.
- `select-profiles.ps1` performs deterministic evidence-based selection and generation.
- `benchmark-presets.ps1` treats candidate configuration as custom authority.
- Harness self-tests cover the mock three-candidate selection path.

No production profile defaults change on this branch yet. The expensive live provider study and human labeling have not been run, so PR #4 must stay draft.

### Resume here

1. Run Task 5b's live study:

   ```powershell
   ./eng/benchmark-presets.ps1 -Experiment eng/benchmark/experiments/profile-candidates-v13.json
   ```

   Expect roughly 32,000 fetches from 18 candidates × 300 rows × 3 repetitions × cold/warm cache modes. Use the harness checkpoint/resume path rather than restarting a partial run.

2. Prepare and label the review queue:

   ```powershell
   ./eng/benchmark/prepare-review.ps1
   ```

   Human labels are `correct`, `acceptable-synthetic`, `generic`, `wrong-brand`, `blank`, `unusable`, or `ambiguous`. Do not commit raw run artifacts or review queues that contain local paths.

3. Select the winners and generate the managed catalog plus evidence report:

   ```powershell
   ./eng/benchmark/select-profiles.ps1
   ```

   Review and commit `FetchProfileCatalog.Generated.cs` and `docs/benchmarks/v1.3-provider-study.md` only after validating the labeled input and generated diff.

4. Finish Plan 02 Task 6: run `eng/export-profile-data.ps1`, add `-Check`, `ProfileExportTests`, and the CI drift gate for `site/data/profiles.json`.

5. Continue sequentially with Plan 03 (guided-native UX), Plan 04 (website completion), and Plan 05 (release validation). Do not begin the UX implementation until the managed catalog is frozen from the study.

### Fresh verification

Run locally on 2026-08-17 before publishing this handoff:

- `dotnet build KeeFetch.csproj --configuration Release --no-restore -warnaserror`: passed with 0 warnings and 0 errors.
- The same Release build with `-p:LangVersion=5`: passed with 0 warnings and 0 errors.
- Release test assembly build: passed with 0 warnings and 0 errors.
- `dotnet test KeeFetch.Tests/KeeFetch.Tests.csproj --configuration Release --no-build`: 116/116 passed.
- `eng/benchmark/test-benchmark-harness.ps1`: passed.
- Versioned public corpus validation through `Test-KeeFetchCorpus`: passed.
- `git diff --check`: passed.

### Required gates before advancing PR #4

- C# 5 Release build with warnings as errors and zero warnings/errors.
- Full solution tests.
- Benchmark harness self-tests, including selection, stratified review, and resume behavior.
- Review of generated files for local paths, raw artifacts, and unsupported claims.
