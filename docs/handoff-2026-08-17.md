# Handoff - 2026-08-17 (supersedes all earlier versions of this file)

- Active branch: `codex/v1-3-profile-selection`
- Draft PR: [#4 - Scaffold v1.3 profile selection tooling, export consistency, and evidence repair](https://github.com/tzii/KeeFetch/pull/4)
- Base: `master`

## Directive authority (read this first)

The governing instruction is the **1185-line correctness/evidence repair directive** issued by the project owner on 2026-08-17. It **supersedes** the earlier repair track described by older commits/status docs in four decisive ways:

1. **Review is a CENSUS, not a sample.** Delete every weighted-sample / Wilson-interval claim from final v1.3 output. Every unique cold `(fixture_id, artifact_hash)` is human-reviewed exactly once; labels propagate to every occurrence.
2. **The currently-running study (any launch before the reworked harness) is methodologically invalid** and must never be merged into future evidence. Kill + quarantine it; never resume old cells against new code.
3. **Execution-semantics repairs are still required** before any study can count: early-stop authority, Android store privacy flag, one downloader per measured cell, atomic warm blocks, real seeded schedule, execution-harness fingerprint, exact matrix validation, strict provider_metrics, one shared winner function with pessimistic/optimistic ambiguity replay, fail-closed publication.
4. **A second agent session may still hold the older, superseded directive** (sampling + Wilson + "everything complete"). Its methodology conclusions are NOT authoritative. Do not merge its quarantined runs. Do not let it relaunch the study before the launch gate below.

## Evidence and study state (all invalid by design)

- The **22:39 study** (partial) and the **00:08 detached relaunch** (PID 16892) were stopped. Full outputs quarantined, never to be merged:
  - `eng/benchmark-runs/quarantine/profile-candidates-v13-invalid-pre-final-methodology-20260817-220136Z/`
  - `eng/benchmark-runs/quarantine/profile-candidates-v13-invalid-pre-final-methodology-relaunch-20260817-221441Z/`
  - plus the older 12:59 and 22:36 quarantine sets.
- **No study is running and NONE may be launched** until the full launch gate passes at one exact revision (next steps, item 5).
- The hourly external monitor `automation-07cd5199` may relaunch a detached study; the owner agreed to disable it. If a relaunch appears: stop the runner, quarantine its output dir and `study.pid`/console logs under `eng/benchmark-runs/quarantine/`, and record it here.
- `bin/Release/net48/KeeFetch.dll` currently contains commit-1 repair code (v2 policy fingerprints incl. `AllowAndroidStoreLookup`) while the harness is still pre-commit-2. Any run produced now is mixed-binary/mixed-harness evidence; the fail-closed pre-scan/selector will reject it. Just quarantine it.

## Committed work (HEAD `d7aaad6`)

**Commit 1 - fix: execution-policy semantics, Android privacy, and HTTP disposal (21 files, +1027/-271):**

- `stopAfterStrongResolved` is now the single early-stop authority; the Direct Site strong-canonical shortcut no longer bypasses `stop=false`. Tests prove: strong Direct Site + stop=false => later provider queried; stop=true => skipped; weak Direct Site => no stop.
- `FetchExecutionPolicy` constructs fail-closed: non-empty unique catalog-resolvable provider ids; integral timeouts in sane ranges; cumulative >= primary. Unknown managed profile ids throw instead of degrading into Custom. `BuildProviderPipeline` throws on unknown ids (defense in depth).
- New behavior-affecting field **`AllowAndroidStoreLookup`**: gates the Google Play lookup (Privacy=false; bulk-fast/everyday/max-coverage=true), part of CanonicalForm (now **v2**)/Fingerprint, exported to `site/data/profiles.json` (**schema 2**), settable via benchmark override `KeeFetch.CustomAllowAndroidStoreLookup` (-1 = derive from `UseThirdPartyFallbacks`).
- Android requests obey **one outer cumulative deadline** shared by domain and Play phases; Play starts only when remaining >= 1000 ms and gets `min(7000, remaining)`; exhaustion recorded as `skipped-budget-exhausted` metric.
- Deterministic `HttpResponseMessage` disposal on every path in `IconProviderBase`, `DirectSiteProvider`, `AndroidAppMapper` (success / HTTP error / oversize / parse rejection / retry); cancellation registration retained only to abort stalled framework stream reads.
- Runner hardened: strict candidate validation (unique ids, catalog provider resolution, real booleans, integral timeouts, `profiles[] == candidates[]` exact set); `New-CustomConfigForCandidate` fails on unknown provider ids and applies `allowAndroidStoreLookup` explicitly. All candidate defs in `profile-candidates-v13.json` and `smoke-distinguish-v13.json` declare the field.
- Tests: 139 -> **160, all green** (early-stop trio, Android privacy/deadline regressions, disposal proofs, policy validation matrix, android-store fingerprint split). `[DoNotParallelize]` on every `SharedHttp`-mutating test class.

## In-flight work (NOT committed; do not lose)

`eng/benchmark/BenchmarkHarness.psm1` (the only dirty file) adds four exported functions for commit 2:

- `Get-KeeFetchHarnessFingerprint -RepoRoot` - versioned (`keefetch-exec-harness-v1`) SHA-256 over `eng/benchmark-presets.ps1` + `eng/benchmark/BenchmarkHarness.psm1`, LF-normalized. Runs must record it; pre-scan and selector require it uniform.
- `Get-SeededScheduleOrder -Items -Seed -Phase -Repetition` - deterministic SHA-256 ranking; replaces `Get-RotatedOrder`; independent of PowerShell's randomized string hashing.
- `Get-WarmBlockInvalidReason -Warmups -Measured -Repetitions` - `$null` when a warm block is one complete atomic candidate block; else a reason string (missing/duplicate/incomplete warm-up or measured repetition).
- `Assert-KeeFetchRunRows` - exact matrix validation: row count == corpus set size; no duplicate/missing/unexpected fixture ids; per-row metadata matches run.json; recorded corpus fingerprint == current bytes; optional `-RequireSuccessArtifacts` for measured cold cells (success rows need non-empty hash + artifact on disk).

NOT yet wired into the runner: one downloader per cell; warm block validation + quarantine-rebuild; seeded schedule + `schedule.json` write/verify; `execution_harness_fingerprint` + environment metadata in run.json; `Assert-KeeFetchRunRows` replacing the tail row-count check; C# provider-health transference test; self-tests in `eng/benchmark/test-benchmark-harness.ps1`.

## Next steps (in order)

1. Finish commit 2 (above), commit, keep gates green (Release -warnaserror, C#5 gate, MSTest 160, harness self-tests, corpus validation, export -Check, git diff --check).
2. Rewrite `prepare-review.ps1` to the cold-artifact census (phase 12) - commit #3.
3. Rework `select-profiles.ps1`: strict provider_metrics parser, cold-only scoring, one shared winner function with pessimistic/optimistic replay, no Wilson, fail-closed publication - commit #4.
4. Expand adversarial self-tests + policy smokes + documentation - commit #5.
5. **Launch gate**: at one exact commit run every check above + smokes; record binary + experiment + corpus + harness fingerprints.
6. Launch the final full `profile-candidates-v13` study from a clean output root (detached OK); do not modify binary/experiment/corpus/harness afterwards.
7. **Human-review gate - mandatory STOP**: generate the complete cold census, report census stats, run `-Validate` only after a human labels the queue. DO NOT fabricate labels. If human review is unavailable this session, STOP and report exactly that; do not select/publish winners.
8. Dry-run selector, inspect all four winners + ambiguity replay, `-Publish`, regenerate `site/data/profiles.json` + `-Check`, update docs, exact-PR-HEAD gates, CI green, mark PR #4 ready, provide the final report.

## Standing rules

- Fail closed: incomplete/stale/mixed-binary/mixed-runner/malformed/invalid evidence never publishes; any weakened validator is a release blocker.
- Recorded policy == executable policy == benchmark policy == documented policy. Any break blocks release.
- Human admissibility: labels never fabricated, never auto-inferred; no guessed winners; never mix old and new evidence cells.