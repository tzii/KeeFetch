# Handoff - 2026-08-17 (supersedes all earlier versions of this file)

- Active branch: `codex/v1-3-profile-selection`
- Draft PR: [#4 - Scaffold v1.3 profile selection tooling, export consistency, and evidence repair](https://github.com/tzii/KeeFetch/pull/4)
- Base: `master`

## Directive authority (read this first)

The governing instruction is the **1185-line correctness/evidence repair directive** issued by the project owner on 2026-08-17. It **supersedes** the earlier repair track described by older commits/status docs in four decisive ways:

1. **Review is a CENSUS, not a sample.** Delete every weighted-sample / Wilson-interval claim from final v1.3 output. Every unique cold `(fixture_id, artifact_hash)` is human-reviewed exactly once; labels propagate to every occurrence.
2. **The currently-running study (any launch before the reworked harness) is methodologically invalid** and must never be merged into future evidence. Kill + quarantine it; never resume old cells against new code.
3. **Execution-semantics repairs are still required** before any study can count: early-stop authority, Android store privacy flag, one downloader per measured cell, atomic warm blocks, real seeded schedule, execution-harness fingerprint, exact matrix validation, strict provider_metrics, one shared winner function with pessimistic/optimistic ambiguity replay, fail-closed publication.
4. **A second agent session may still hold the older, superseded directive** (sampling + Wilson + "everything complete"). Its methodology conclusions are NOT authoritative. Do not merge its quarantined runs. Do not let it relaunch the study before the launch gate below.

## Evidence and study state (all invalid by design)

- The **22:39 study** (partial) and the **00:08 detached relaunch** (PID 16892) were stopped. Full outputs quarantined, never to be merged:
  - `eng/benchmark-runs/quarantine/profile-candidates-v13-invalid-pre-final-methodology-20260817-220136Z/`
  - `eng/benchmark-runs/quarantine/profile-candidates-v13-invalid-pre-final-methodology-relaunch-20260817-221441Z/`
  - plus the older 12:59 and 22:36 quarantine sets.
- **No study is running and NONE may be launched** until the full launch gate passes at one exact revision (next steps, item 5).
- The hourly external monitor `automation-07cd5199` may relaunch a detached study; the owner agreed to disable it. If a relaunch appears: stop the runner, quarantine its output dir and `study.pid`/console logs under `eng/benchmark-runs/quarantine/`, and record it here.
- `bin/Release/net48/KeeFetch.dll` currently contains commit-1 repair code (v2 policy fingerprints incl. `AllowAndroidStoreLookup`) while the harness is still pre-commit-2. Any run produced now is mixed-binary/mixed-harness evidence; the fail-closed pre-scan/selector will reject it. Just quarantine it.

## Committed work (HEAD `c2e41cb`)

**Commit 4 - feat: select-profiles cold-census rework (`c2e41cb`):**

- **Strict provider_metrics parser**: rows must carry a JSON array of metric objects with known provider names (catalog display names + Cache/Coalesced/Pipeline/Google Play/Harness) and known outcomes, integral non-negative counts (calls >= 1); a successful fetch with zero recorded provider activity fails the selection. Also fixes a latent PS 5.1 mis-parse (top-level JSON arrays returned unenumerated, so multi-provider rows were silently string-coerced).
- **Cold-only scoring**: every scored quantity (availability, label rates, provider calls, third-party disclosure, latency, batch speed) is computed over measured cold cells; warm rows appear only as informational latency percentiles.
- **Census queue equality**: the selector regenerates the cold census independently and requires the queue to equal it exactly (missing unit or non-census key fails closed). Label rates are exact census proportions; stratum weights, targeted/sampled counts, and all Wilson-interval output are deleted from code and generated reports.
- **One shared winner function** (`Invoke-WinnerRule`): every role is replayed under pessimistic (ambiguity-as-failure) and optimistic (ambiguity-as-usable) scenarios; unstable or replay-emptiable rules reject the whole selection. Bulk-fast thresholds and everyday normalization are scenario-scoped.
- **Provenance gate**: runs must carry a uniform `execution_harness_fingerprint` matching the selecting repository's harness; missing/mixed/stale fails closed.
- **Fail-closed -Publish**: all four OutputDir artifacts must exist and each repository write is read back and byte-compared.
- Self-tests cover census counts, no-Wilson/no-sampling-field output, and the fail-closed paths (hole, fabricated key, malformed/unknown provider/unknown outcome/zero-activity metrics, stale harness fingerprint).

**Commit 3 - feat: prepare-review cold-artifact census (`2aaaead`):** review is a census over unique cold `(fixture_id, artifact_hash)` units; sampling/strata/weights/seed deleted; `-Validate` regenerates the census and fails closed on holes and non-census keys.

**Repair - fix: reflection-based catalog resolution and JSON writer export (`c689bed`):** bracket type literals for `FetchProfileCatalog` do not resolve for LoadFrom-context assemblies in a fresh process (reproduced); resolved via reflection like the rest of the script. `Write-JsonFileUtf8NoBom` exported from the harness module (schedule.json writer is called cross-module). Both files are harness-fingerprint components; no live evidence existed to invalidate.

**Commit 2 - feat: seeded schedule, harness fingerprints, atomic warm blocks, exact matrix validation (4 files, +654/-57):**

- **One FaviconDownloader per matrix cell** in `benchmark-presets.ps1`: provider-health state transfers within a cell and resets between cells (production-representative); proven by the new MSTest `ProviderHealth_TransfersWithinOneDownloader_AndResetsAcrossInstances` (tests 160 -> **161, all green**).
- **Seeded schedule**: `Get-SeededScheduleOrder` (SHA-256 ranking over `keefetch-schedule-v1|seed|phase|rep|item`) replaces the rotation; experiments must declare `schedule_seed` explicitly (fail closed otherwise).
- **schedule.json** in the output root records seed + experiment/corpus/binary/harness fingerprints + the exact cell order; every later invocation over the root verifies it byte-for-cell or fails closed.
- **run.json records `execution_harness_fingerprint`** (`keefetch-exec-harness-v1` over runner + module, LF-normalized) plus harness environment; the pre-scan rejects runs from a different/missing harness fingerprint.
- **Atomic warm blocks**: `Get-WarmBlockInvalidReason` validates one complete warm-up + complete measured reps 1..N; any deviation moves the candidate's whole warm block into `eng/benchmark-runs/quarantine/<exp>-warmblock-rebuild-<cand>-<stamp>/` (with `quarantine-reason.txt`) and the schedule rebuilds it from scratch. Warm cells are never resumed individually. Fixed an unreachable-branch bug: empty collections now bind via `AllowEmptyCollection`.
- **`Assert-KeeFetchRunRows`** replaces row-count-only checks in both the runner tail and the pre-scan: exact fixture set (no duplicate/missing/unexpected), per-row metadata vs run.json, corpus fingerprint freshness, and `-RequireSuccessArtifacts` (non-empty hash + artifact on disk) for measured cold cells.
- Self-tests 8-11 in `eng/benchmark/test-benchmark-harness.ps1` cover all four functions (schedule determinism/sensitivity, warm-block invalidity matrix, fingerprint stability/CRLF invariance/change sensitivity, exact-row accept/reject).

**Commit 1 - fix: execution-policy semantics, Android privacy, and HTTP disposal (`d7aaad6`, 21 files, +1027/-271):**

- `stopAfterStrongResolved` is now the single early-stop authority; the Direct Site strong-canonical shortcut no longer bypasses `stop=false`. Tests prove: strong Direct Site + stop=false => later provider queried; stop=true => skipped; weak Direct Site => no stop.
- `FetchExecutionPolicy` constructs fail-closed: non-empty unique catalog-resolvable provider ids; integral timeouts in sane ranges; cumulative >= primary. Unknown managed profile ids throw instead of degrading into Custom. `BuildProviderPipeline` throws on unknown ids (defense in depth).
- New behavior-affecting field **`AllowAndroidStoreLookup`**: gates the Google Play lookup (Privacy=false; bulk-fast/everyday/max-coverage=true), part of CanonicalForm (now **v2**)/Fingerprint, exported to `site/data/profiles.json` (**schema 2**), settable via benchmark override `KeeFetch.CustomAllowAndroidStoreLookup` (-1 = derive from `UseThirdPartyFallbacks`).
- Android requests obey **one outer cumulative deadline** shared by domain and Play phases; Play starts only when remaining >= 1000 ms and gets `min(7000, remaining)`; exhaustion recorded as `skipped-budget-exhausted` metric.
- Deterministic `HttpResponseMessage` disposal on every path in `IconProviderBase`, `DirectSiteProvider`, `AndroidAppMapper` (success / HTTP error / oversize / parse rejection / retry); cancellation registration retained only to abort stalled framework stream reads.
- Runner hardened: strict candidate validation (unique ids, catalog provider resolution, real booleans, integral timeouts, `profiles[] == candidates[]` exact set); `New-CustomConfigForCandidate` fails on unknown provider ids and applies `allowAndroidStoreLookup` explicitly. All candidate defs in `profile-candidates-v13.json` and `smoke-distinguish-v13.json` declare the field.
- Tests: 139 -> **160, all green** (early-stop trio, Android privacy/deadline regressions, disposal proofs, policy validation matrix, android-store fingerprint split). `[DoNotParallelize]` on every `SharedHttp`-mutating test class.

## In-flight work

None - the working tree is clean at `c2e41cb` (only the untracked `.agent/` task-state files and `AGENTS.md` may be pending). Commit-4 gates: harness self-tests green (parser + selector + census + fail-closed paths), parse checks, `git diff --check`. No C# was touched, so the MSTest suite is unaffected by the commit; run it at the launch gate as usual.

Operational note: `eng/benchmark-runs/smoke-policy-check/` still holds three runs from the pre-fingerprint harness; any rerun against that root now fails closed by design. Quarantine it (or use a fresh output root) before the next smoke.

## Next steps (in order)

1. ~~Finish commit 2~~ - DONE as `9ff7d60` (all gates green).
2. ~~Rewrite prepare-review.ps1 to the cold-artifact census~~ - DONE as `2aaaead`: every unique cold `(fixture_id, artifact_hash)` unit reviewed exactly once, no sampling/strata/weights/seed, cold-cells-only scope, `-Validate` regenerates the census and fails closed on any hole or non-census key.
3. ~~Rework select-profiles.ps1~~ - DONE as `c2e41cb`: strict provider_metrics parser, cold-only scoring, census queue equality, one shared winner function with pessimistic/optimistic ambiguity replay, no Wilson anywhere, uniform+current execution-harness fingerprint gate, fail-closed verified `-Publish`.
4. Expand adversarial self-tests + policy smokes + documentation - commit #5 (next).
5. **Launch gate**: at one exact commit run every check above + smokes; record binary + experiment + corpus + harness fingerprints.
6. Launch the final full `profile-candidates-v13` study from a clean output root (detached OK); do not modify binary/experiment/corpus/harness afterwards.
7. **Human-review gate - mandatory STOP**: generate the complete cold census, report census stats, run `-Validate` only after a human labels the queue. DO NOT fabricate labels. If human review is unavailable this session, STOP and report exactly that; do not select/publish winners.
8. Dry-run selector, inspect all four winners + ambiguity replay, `-Publish`, regenerate `site/data/profiles.json` + `-Check`, update docs, exact-PR-HEAD gates, CI green, mark PR #4 ready, provide the final report.

## Standing rules

- Fail closed: incomplete/stale/mixed-binary/mixed-runner/malformed/invalid evidence never publishes; any weakened validator is a release blocker.
- Recorded policy == executable policy == benchmark policy == documented policy. Any break blocks release.
- Human admissibility: labels never fabricated, never auto-inferred; no guessed winners; never mix old and new evidence cells.