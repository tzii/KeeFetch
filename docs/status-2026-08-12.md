## Status — 2026-08-12

> **Superseded 2026-08-17** — the harness/selector methodology described below (10% stratified self-test, "deterministic weighted scoring") was replaced by the cold-artifact CENSUS directive; see `docs/handoff-2026-08-17.md` for the authoritative current state. Kept as a historical snapshot.

Branch `codex/v1-3-profile-selection` pushed at `ab9e1f0`.

### Gates (all green locally)
- C# 5 Release: 0 warn/error
- Tests: 116/116 (includes RegressionFixtures_AllFourFilesPresent)
- Harness self-tests: passed (incl. mock 3-candidate + 10% stratified + resume)
- CI last: PR #3 merged at 13fd9b8 (checks: build pass)

### Done
- **PR #3 merged** — Plans 01 (all 6) + 02 tasks 1-4. Includes fixes for:
  PLGX staging (FetchProfiles), managed-profile authority, concurrency/resume,
  harness-error vs not-found, artifacts+hash, KDBX force-add, repetition bug,
  privacy UI.
- **Task 5a scaffold** (ab9e1f0) — `profile-candidates-v13.json` (18 custom
  candidates with explicit providerIds/timeouts/synthetic), `prepare-review.ps1`
  (queue+Validate), `select-profiles.ps1` (deterministic weighted scoring),
  candidate custom-config authority via Load-CandidateMap/New-CustomConfigForCandidate.

### What to do next

**1. Live study (Task 5b) — not yet run**
- Run: `benchmark-presets.ps1 -Experiment eng/benchmark/experiments/profile-candidates-v13.json`
- 18 candidates x 300 rows x 3 reps x 2 cache_modes = ~32k fetches (hours)
- Then: `prepare-review.ps1` -> human label `review-queue.csv` (correct/acceptable-synthetic/generic/wrong-brand/blank/unusable/ambiguous)
- Then: `select-profiles.ps1` -> `FetchProfileCatalog.Generated.cs` + `docs/benchmarks/v1.3-provider-study.md`
- Do NOT commit raw artifacts/review queues with local paths

**2. Task 6 — cross-surface consistency**
- `eng/export-profile-data.ps1` (-Check) -> `site/data/profiles.json`
- `ProfileExportTests` + CI gate

**3. Plan 03 — guided-native UX** (after catalog frozen)
- SettingsDraft, 4 TabControl pages, FirstRunForm, CompletionForm, BatchRunResult/BatchProgress

**4. Plan 04 — website** (7 pages + verify-site.py + smoke-site.ps1)

**5. Plan 05 — release validation** (version freeze, PLGX load-test, hashes)

### Decisions
- Candidate configs are custom (Task 5a fix); managed profiles are winners only
- Live study deferred: scaffold first, then evidence; deferring keeps history clean

Head: ab9e1f0 on codex/v1-3-profile-selection (parent 13fd9b8 master)
