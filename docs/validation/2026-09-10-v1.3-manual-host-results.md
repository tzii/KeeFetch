# v1.3 manual host acceptance

Decision: **GO WITH CONDITIONS** — functional host checks pass by the owner's report. Four genuine screenshots, their website integration and final release review/publication remain. This records the owner's observations; Codex did not operate or visually inspect the KeePass host.

## Scope and identity

The prepared KeePass 2.60 hosts use candidate source `9f0b43f64335803d24ca99d6ccc7e0e7abac3bb8`, version 1.3.0.0. The retained DLL and PLGX hashes are unchanged, and the installed `portable-plgx/Plugins/KeeFetch.plgx` matches the retained PLGX. Subsequent PR commits change tests/evidence only; automated CI on `19a0c22` is green.

The owner supplied complete and cancelled Fast batch summaries, stated “ALL OTHER TESTS seem fine”, and explicitly confirmed that this includes both DLL and PLGX, all four display scales, High Contrast, migration cases, upgrade/rollback and the one-time Retry Eligible test: **“Yes, I tested all of those.”** No more functional retesting is requested unless a new defect or product change requires it.

| Checklist area | Acceptance |
|---|---|
| DLL/PLGX loading, first run, Save/Cancel and persistence | PASS — owner reported |
| Keyboard navigation and actions | PASS — owner reported |
| Actual 100%, 125%, 150% and 200% display scaling | PASS — explicitly confirmed |
| Windows High Contrast | PASS — explicitly confirmed |
| Mixed completion, cancellation, retry, skipped/success-only/Recycle Bin cases | PASS — owner reported; summary arithmetic independently checked below |
| Long diagnostics path | PASS — covered by the owner's all-other-tests report |
| Legacy Fast/Balanced/Thorough/Custom/unknown migration | PASS — explicitly confirmed |
| Authentic v1.2 upgrade and restoration of plugin/configuration/database backups | PASS — explicitly confirmed |
| Four final-host screenshots | PENDING — none received or found in the candidate/media directories |

## Reported batch outcomes

| Field | Completed run | Cancelled run |
|---|---:|---:|
| Profile | Fast (`bulk-fast`) | Fast (`bulk-fast`) |
| Elapsed | 22.071 s | 2.314 s |
| Total | 71 | 71 |
| Updated | 50 | 10 |
| Skipped | 0 | 0 |
| Not found | 15 | 1 |
| Errors, including invalid input | 6 | 0 |
| Invalid input, already included in errors | 2 | 0 |
| Cancelled entries | 0 | 60 |
| Retry eligible | 19 | 1 |
| Run cancelled | No | Yes |

Both totals reconcile: `50 + 15 + 6 = 71` and `10 + 1 + 60 = 71`. The completed retry count is `15 + (6 - 2) = 19`; invalid input is excluded. The cancelled summary's single retry-eligible entry is its earlier miss, not one of the 60 cancelled entries. The UI intentionally disables retry for the entire cancelled run even though the summary retains that count. The arithmetic does not establish the causes of the four non-invalid errors or a fixed expected live-site success rate.

## Preserved evidence and limits

Evidence directory: `artifacts/v1.3-preparation/candidate-9f0b43f-20260910/manual-host-review-20260910-153910/`. It contains the supplied summaries, explicit confirmation, a hash-bound acceptance record, and snapshots of the available log and CSV. Candidate files and original diagnostics were not overwritten.

The complete-run diagnostic paths as pasted (`KeeFetch/_diagnostics.log` and `.csv`) were absent. The available `portable-plgx/KeeFetch_diagnostics.csv` has nine fetch rows: eight Success and one NotFound. Those rows do not fully match either supplied summary. The exporter reuses and overwrites the same filenames between runs, so these snapshots are not claimed as exact per-run reconciliation. The owner-reported functional acceptance and independently verified summary arithmetic remain distinct from the retained partial diagnostics. No error cause or missing row is invented.

## Remaining release work and risk

**Medium:** final media remains incomplete. Capture Settings Overview, Settings Providers, First Run and Completion from the synthetic candidate host, then integrate them as `settings-overview.png`, `settings-providers.png`, `first-run.png` and `completion-summary.png` in `site/assets/media/`. Validate the changed website. Keep unrelated ongoing website edits outside this candidate unless separately integrated and checked.

No database-format migration is introduced. Legacy configuration migration and upgrade/rollback are accepted from the owner's tests. Recovery remains: close KeePass and restore the saved plugin plus affected configuration/database backups; replacing the plugin alone does not undo downloaded icons.

Rollout after the media gate: finish release review, merge the prepared PR, tag v1.3.0, publish the exact retained DLL/PLGX and checksum manifest, deploy GitHub Pages and verify downloaded hashes/links. CI rebuild artifacts must not replace the retained packages. The release notes provide installation, migration, privacy and recovery guidance for users; the validation record provides evidence and remaining gates for maintainers. Nothing has been merged on GitHub, tagged or published by this acceptance update.
