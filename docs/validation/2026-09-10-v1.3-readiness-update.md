# v1.3 readiness update — September 10, 2026

Historical checkpoint, superseded by [final selection and integration](2026-09-10-v1.3-selection-and-freeze.md): adjudication and the full spot-check are now complete; the owner approved conservative failure scoring after the strict replay rejected Precise. The findings below describe the earlier audit.

Decision: **NO-GO for release.** Base census delivery and structural validation are complete. Final label adjudication, profile selection, version freeze, current-source CI, exact-artifact host validation and publication remain open.

Scope remains the guided first-run/settings/completion UX, networking and cancellation fixes, expanded provider study, seven-page GitHub Pages site and v1.3 packaging.

## What closed today

- Independently validated all 19 delivered ZCode batches: 267 exact keys, allowed labels, image-inspection assertions, prompt identity, unchanged manifest/image bytes, accepted assignment/pilot identities and nonempty reconstructed transcripts. Reported totals reproduce: 250 correct, 7 acceptable-synthetic, 6 wrong-brand, 2 generic, 1 blank, 1 ambiguous; 91 marked live-verified by the reviewer.
- Combined those with the separate 560 agy labels. The collector passes all 827 keys, rejects duplicate sources and verifies same-image/domain consistency. `prepare-review.ps1 -Validate` passes the separate 827-row machine candidate queue. The full 133-cell matrix was independently reverified; no collection rerun is needed.
- Applied the nine previously accepted corrections from reasks 01–04 to a new `review-queue-adjudicated-01.csv`. Original reviews and the study's actual `review-queue.csv` are unchanged. This remains a candidate, not an accepted selection input.
- Inspected 21 original images covering 26 selected units, including all 17 non-correct ZCode labels and one correct example per available category. Compared four official reference images directly. This is partial machine verification, not human or final spot-check acceptance.
- Fixed evidence-parser compatibility for the delivered before/after hash layouts, additional hashes of explicitly permitted metadata files, and annotated timestamps. Every supplied digest is checked; raw timestamp annotations remain in imported provenance. Original evidence was not rewritten. All **57 evidence tests** pass, including seven new validator regressions. Initial unittest discovery found no tests because these filenames contain hyphens; executing the five test scripts directly ran the complete evidence test set.

Evidence: `artifacts/v1.3-preparation/audit-20260910/final-validation.json` and `official-references/references.json`; combined output in `artifacts/v1.3-preparation/complete-study-review-20260908/census-validation.json`. The final report explicitly supersedes the preserved intermediate schema-failure report. The collector's 38-unit sheet is only an initial sample; the expanded adjudicator selects 138 units before reconciliation with existing observations. That number is not a count of unseen images.

Provenance remains owner-run, harness-reported ZCode model identity with reconstructed transcripts and some approximate timestamps. Structural checks do not independently attest the underlying model, fresh conversation isolation, or every claimed live lookup.

## What still blocks the study gate

Seven newly inspected labels have contrary official evidence and require disclosed correction/review before selection:

| Batch / units | Current label | Direct evidence |
|---|---|---|
| 21b / three badssl units, hash `cdb4554c…` | acceptable-synthetic | The white lock with X on red matches [badssl's own icon](https://badssl.com/icons/icon-red.png). A failed guessed favicon URL did not establish a synthetic image. |
| 24a / Zoom, hash `e3cce267…` | wrong-brand | The white `zm` icon is Zoom's own mark, directly compared with the [favicon linked by Zoom](https://st1.zoom.us/homepage/20260825-1235/primary/favicon.ico). |
| 25a / Paul Graham, hash `ecd60ddc…` | generic | Decoded pixels are identical to the site's [32×32 favicon](https://www.paulgraham.com/favicon.ico). |
| 25a / Paul Graham, hash `f048c030…` | generic | The enlarged blue/white/red/black arrangement matches the Arc icon explicitly linked by [paulgraham.com](https://www.paulgraham.com/). |
| 26b / Paul Graham, hash `fbb08780…` | wrong-brand | Decoded pixels are identical to the [18×18 Arc icon](https://ycombinator.com/arc/arc.png) declared by that site. |

Additional focused review remains for NU.nl's unproven synthetic attribution, the Office/Copilot icon, and specific Amazon Shopping identity. [Microsoft documents the Office web app transition](https://support.microsoft.com/en-us/microsoft-365-copilot/the-microsoft-365-app-transition-to-the-microsoft-365-copilot-app), so the old orange Office O alone is not a valid rejection reference. The two earlier orange-smile Shopping units in reask-05 remain untouched and unresolved; the new black-`a` Shopping sample also needs app-specific attribution. NeverSSL's cyan/white image was viewed: it has no recognizable mark, but its blank label is not proof of transparency.

Next study sequence: finish focused adjudication → reconcile the full required direct spot-check (all non-correct, all Android-app units, adjudications and category samples) → validate exact final queue identity → select four profiles without publication and require identical winners with ambiguity treated as failure versus usable. Preserve all raw labels and source distinctions. Do not resume the obsolete agy base-census assignments.

## Remaining release gates

| Severity | Missing work | Evidence needed to close |
|---|---|---|
| Medium | Final study adjudication and selection | Corrected, provenance-preserving final queue; completed direct spot-check; stable four-profile ambiguity replay. |
| High | Final catalog and v1.3 metadata | Generate accepted catalog/report/JSON/README/site; set assembly and update feed to 1.3.0.0; finalize changelog and release notes. Fresh tag check still fails on 1.2 metadata and missing `[1.3.0]` changelog section. |
| High | Integrated-source gates and review | Commit intended integration, review its actual diff, run Release `-warnaserror`, full MSTest, Windows PowerShell harness self-tests, export `-Check`, version/manifest/workflow checks and `git diff --check`; obtain green CI on that exact source. |
| High | Final artifacts and real KeePass validation | Retain exact final DLL/PLGX and SHA256SUMS; verify each format in KeePass 2.60, installation, upgrade, configuration persistence and rollback. Complete keyboard, actual 125/150/200% DPI, High Contrast, migrations, outcome/retry and long-path checks. |
| Medium | Final media and website integration | Capture the validated candidate, insert final profile content, rerun affected site checks and integrate the GitHub Pages build. Prior website 63-case/16-test evidence is historical, not repeated in this update. |
| High | Review/merge and release rollout | After the concrete candidate is green, complete review and publication decisions, tag, publish the exact validated assets, deploy GitHub Pages, and verify downloaded hashes and a fresh downloaded-plugin load. |

Fresh GitHub check: [PR #8](https://github.com/tzii/KeeFetch/pull/8) is OPEN/DRAFT at `18d49093d10e0dd819eea8a21e52cad4cc91f6ac`; its green CI is from September 7. Local HEAD remains `cac2cb07361a99626dd3463697907e1ac84f0805` with uncommitted preparation. Master remains `f252f8d6d6aecf7a4f114fc34855c3c7eb93c366`; latest release is v1.2.0 and v1.3.0 has no tag. The existing green CI does not cover this work.

No production C# or study input changed in this update. The frozen DLL hash is still `415F66BC2190D4FFB71E48FACC08763C6D0F37DCCDFC3B6788A8176194FC846D`. The September 9 full-suite checkpoint remains 236 passed / one v1.3-version failure / zero skipped; no new full C# suite is claimed today. `git diff --check` passes. Open dependency maintenance and the documented DNS/log-redaction limitations are not newly added release blockers.

## Migration, rollout and recovery

No database-format migration is introduced. Configuration migration remains Fast → Fast, Balanced → Balanced, Thorough → Precise, Custom/unknown → Custom; preserving Custom settings and repeat-launch idempotency needs final-host evidence.

Rollout order: finish study → freeze catalog/version → integrate and pass source gates → package/test exact bytes and capture media → complete review/publication decisions → publish and verify GitHub release/Pages/downloads. Keep stable download links on v1.2 until v1.3 assets exist.

Before upgrade, close KeePass and back up configuration/database and the old plugin. On load failure, configuration loss or incorrect icon changes, close KeePass and restore the old plugin plus affected backups. Plugin rollback alone cannot undo downloaded icons. Validate this recovery on the final candidate.

Maintainer handoff before publication: exact source/asset identities, gate results and remaining decisions. User-facing release notes at publication: final profiles, migration, provider/privacy disclosures, installation and recovery. No external messages or publication were performed.

See the [candidate validation record](v1.3-release-validation.md), [host checklist](v1.3-host-checklist.md) and [study review record](../benchmarks/v1.3-complete-study-review-record.md).
