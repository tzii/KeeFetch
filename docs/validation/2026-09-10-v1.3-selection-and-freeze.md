# v1.3 final study selection and release integration

Current decision: **GO WITH CONDITIONS.** The owner subsequently confirmed all functional host checks; see [manual host acceptance](2026-09-10-v1.3-manual-host-results.md). All four genuine screenshots and their website integration are complete; see [media acceptance](2026-09-10-v1.3-media-intake.md). Current-head media CI and final release review/publication remain. The chronology below preserves earlier native-access failures and pre-acceptance NO-GO decisions as historical evidence. This record supersedes the initial [September 10 readiness audit](2026-09-10-v1.3-readiness-update.md). Scope is the guided KeePass UX, networking fixes, expanded study, managed profiles, documentation website and v1.3 packaging.

## Accepted evidence

The complete matrix contains 133 verified cells: 114 measured cells and 19 warm-ups, covering 19 candidates, 300 fixtures, cold/warm modes and three repetitions. The original study queue, binary, corpus, experiment, execution harness and images remain unchanged through selection.

All 827 base labels were structurally validated with separate provenance: 560 agy labels and 267 owner-run ZCode labels. ZCode model identity remains harness-reported; its transcripts are reconstructions and some timestamps approximate. No human-labeling claim is made.

The final candidate applies accepted reasks 01–05 and a disclosed direct Codex adjudication. It records 23 adjudication entries, including unchanged judgments. Direct comparisons corrected the badssl, Zoom, Paul Graham, NU.nl and Office/Copilot classifications. Every required spot-check unit has an original-image observation: **138 units / 92 images**, with zero missing units. Earlier observations were reconciled against final labels; generating a sheet alone was not counted as inspection.

Final label totals: **762 correct, 17 acceptable-synthetic, 29 generic, 11 wrong-brand, 2 blank, 2 unusable and 4 ambiguous**. The uncertain units are three Amazon Shopping variants and one Reddit Android-app variant. Their exact app identities remain unresolved.

Evidence under `artifacts/v1.3-preparation/complete-study-review-20260908/`:

- `review-queue-final-candidate.csv`, SHA-256 `4111baf62ce983eeeca20548fb3cc5feaa2caee9ae5654eb3995800b0107183b`.
- `review-queue-final-candidate.json`, preserving old/new labels, reasons, hashes and separate reviewer identities.
- `final-spot-check-acceptance.json`, binding the queue and all observation records.

All 63 Python evidence tests pass. The benchmark harness self-tests pass with new conservative-scoring coverage; its default strict rejection and all census/matrix/provenance checks remain enforced.

## Explicit scoring decision

The original strict selector rejected Precise: as-reviewed and ambiguity-as-usable chose `cand-direct-google-twenty-fast`, while ambiguity-as-failure chose `cand-direct-google-balanced`. The preserved Windows PowerShell 5.1 diagnostic is `artifacts/v1.3-preparation/selection-diagnostic-ps51-20260910/rejection.json`. An earlier PowerShell 7 diagnostic failed at its JSON-array compatibility check and is not selection evidence.

After this rejection was explained, the owner explicitly selected **"Use conservative failure scoring (recommended)"** in this task on September 10. The accepted policy is `-AmbiguityPolicy ConservativeFailure`: uncertain labels remain ambiguous, receive zero usability credit, and stay in the denominator. Every role and the reported coverage use that pessimistic usability rate. Wrong-brand labels and their non-ambiguous denominator remain unchanged. All three replay outcomes are retained, including a false stability flag for Precise.

This is a disclosed decision made after observing sensitivity, not a claim that the original stability gate passed. The two leading precision candidates are close: conservative usability is 341/347 (98.2709%) for Direct Site + Google and 380/387 (98.1912%) for Direct Site + Google + Twenty Icons. The study does not establish a broad or statistically robust superiority claim.

The authorization record `artifacts/v1.3-preparation/conservative-policy-acceptance-20260910.json` binds the exact queue, prior spot-check and rejected strict replay. It supersedes only the earlier spot-check statement requiring ambiguity stability. Raw observations and labels are preserved.

Automatic approval review rejected reask-06's external crew execution because its snapshot could export repository context/local review data. It was not retried or bypassed. Codex completed the remaining adjudication locally with original images and official public references; those judgments are explicitly attributed to Codex. No permission request remains for that rejected route.

## Integration and remaining gates

The conservative selector passed without publication; its winners, exact queue hash, policy, replay results and generated descriptions were inspected. A second full run with `-Publish` produced the same four winners and verified the local catalog/report writes. Evidence is retained in `selection-conservative-20260910` and `selection-published-20260910` under the preparation root.

| Profile | Selected candidate | Provider chain | Primary / fallback / total budget |
|---|---|---|---|
| Fast | `cand-direct-yandex-balanced` | Direct Site → Yandex | 6 / 3.5 / 22 s |
| Balanced | `cand-direct-google-twenty-fast` | Direct Site → Google → Twenty Icons | 4 / 2.5 / 15 s |
| Privacy | `cand-direct-only-balanced` | Direct Site | 6 / 3.5 / 22 s |
| Precise | `cand-direct-google-balanced` | Direct Site → Google | 6 / 3.5 / 22 s |

All four selected profiles disable synthetic fallbacks and enable early stop. Privacy disables Android store lookup; the other three enable it. Stable configuration IDs and legacy migration mappings are retained. Fast wins on measured active cold-batch duration; the budget is an upper bound, not a latency measurement.

Assembly, file and update-feed versions are now 1.3.0.0, with the 1.3.0 changelog entry. The catalog, exported JSON, README, release notes and website have been synchronized, including disclosure of the post-review conservative policy. Relevant policy/pipeline regressions were updated to the accepted chains and budgets.

The first build exposed saved candidate catalogs being discovered as production C# under the ignored artifacts directory. `KeeFetch.csproj` now excludes that directory from SDK default inputs; the evidence files remain intact. The subsequent C# 5 Release build and test build passed with **zero warnings/errors**. The full suite passed **237/237, zero skipped**, recorded in `KeeFetch.Tests/TestResults/v13-final-catalog-20260910.trx`. The full Windows PowerShell benchmark harness, export `-Check`, tag-version, 56-item PLGX manifest, release-workflow, site synchronization and whitespace checks pass. The site verifier's **16 tests** and seven-page source/build verification pass; the fresh Edge matrix passed all **63 cases** (normal, forced colors, reduced motion, enlarged text and scripts disabled).

Historical pre-commit DLL SHA-256: `f03a728c1a5facd54a7f09d1607d78e781ef51179785b609dc7ec9e6a775bf8b`. The committed and retested package identity below supersedes this build. The original study DLL remains preserved separately.

Source selection, package identity and automated CI are complete. High-severity remaining release gates are installation/upgrade/rollback and visible KeePass host checks. Medium-severity remaining gates are genuine final screenshots and their website integration. Earlier native host results do not establish a final candidate pass. The computer-use runtime refused this KeePass host.

The owner-approved candidate is on draft [PR #8](https://github.com/tzii/KeeFetch/pull/8). CI passes on correction `71ee3f9`: [run 34490006129](https://github.com/tzii/KeeFetch/actions/runs/34490006129). Production source remains identical to packaged `9f0b43f`; later commits record evidence and fix the test fixture. Latest published release is v1.2.0. No merge, tag or release publication occurred.

## Migration, rollout, recovery and communication

No database-format migration is introduced. Legacy Fast/Balanced/Thorough map to Fast/Balanced/Precise; Custom and unknown values map to Custom. Final-host checks must establish that Custom settings survive and repeated launches are idempotent.

Rollout: finish accepted selection, freeze source and version, pass source/CI gates, retain and test exact package bytes, capture real candidate screenshots, complete review, publish those verified assets, then deploy GitHub Pages and check downloaded hashes. Stable download links stay on the existing release until v1.3 assets are available.

Recovery: close KeePass, restore the backed-up plugin and affected configuration/database. Replacing the plugin alone does not undo downloaded icons. Final-host rollback must demonstrate this recovery on synthetic data.

Maintainer communication is the concrete validation packet with source identity, checksums and gate results. User-facing communication is the release notes, migration instructions, privacy limits and installation/recovery guidance published with the release. No external messages have been sent.

## Packaged candidate and execution limits

Candidate 9f0b43f committed on site/redesign-v1-3-release; committed Release/test builds warning-free and 237/237 MSTests (zero skipped). Exact DLL/PLGX packaged with checksums. Native computer-use rejected KeePass: product policy blocks this app. Manual host checks/media remain pending. The owner subsequently explicitly approved the public upload and PR edit. Both completed; the corrected candidate CI passes. Latest release remains v1.2.0; no merge, tag or publication.

The candidate directory is `artifacts/v1.3-preparation/candidate-9f0b43f-20260910`; `candidate.json` binds the source commit, KeePass version, packaging time and all 56 manifest input hashes. `HOST-CHECKS.md` identifies the exact packages and prepared synthetic cases. The post-commit DLL digest changed from the earlier build, so these committed bytes were retested: 237 passed, zero failed/skipped, in `KeeFetch.Tests/TestResults/v13-commit-9f0b43f-20260910.trx`. The pre-commit digest is historical.

The initial packaging attempt exposed null entries in the preparatory script's XML enumeration. No candidate bytes were created. Its failed temporary stage was preserved; the script was corrected to enumerate exactly 56 unique nonempty file nodes and use a fresh stage outside the repository. Packaging then succeeded. Five separate migration hosts, a 211-character database path and an authentic v1.2.0 upgrade/rollback host with backups are prepared. The old PLGX matches the published checksum `1fd7e12590bfc2d23ede93c1ae5e61de0662187321caccdfeeb0bbcb995f5275`. No real vault was used.

The computer-use launch of the synthetic KeePass host returned `product policy blocks this app`; no alternative tool was used to circumvent it. Actual DPI/High Contrast, keyboard, installation, migrations, outcomes, retry, rollback and genuine screenshots remain unverified. See the [final host checklist](v1.3-host-checklist.md).

Automatic approval review initially rejected pushing the source and editing PR #8, citing sensitive external egress and insufficiently specific authorization; those rejected commands did not run. Read-only checks verified the public `https://github.com/tzii/KeeFetch.git` origin and ADMIN permission. After the owner explicitly replied "i approve, please finish work" to the concrete upload request, the exact candidate was pushed and the PR title/body updated. The rejection was resolved by explicit approval, without bypass.

The checkout moved from the study branch to `site/redesign-v1-3-release` immediately before the integration commit, with the same `cac2cb0` parent; that branch choice was preserved. The bundled font license's trailing whitespace was normalized during staged-diff checks without changing its terms or browser runtime assets. No website deployment or release publication occurred. **NO-GO** remains for final host checks/media and release review. Migration, rollout, rollback and communication instructions above still apply.

## Approved upload and CI correction

The initial run on `9f0b43f`, [34489522505](https://github.com/tzii/KeeFetch/actions/runs/34489522505), failed four prompt-policy tests. GitHub's Windows temporary directory used a short path alias; the fixture wrote that unresolved alias while the unchanged validator expected the resolved batch directory. The same four failures were reproduced locally using a Windows short temporary path. Resolving the fixture root, as real batch generation already does, makes all 63 evidence tests pass in that environment. Strict prompt, hash and provenance rejection behavior is unchanged.

The corrected [CI run](https://github.com/tzii/KeeFetch/actions/runs/34490006129) passes the evidence/site checks, Release and C# 5 builds, all 237 MSTests, profile export, full benchmark harness, PLGX packaging and checksum artifact generation. The tag-only release verification job is correctly skipped on a PR. CI-generated packages are verification artifacts; they do not replace the retained local DLL/PLGX selected for host acceptance.

All 56 original packaging input hashes and both candidate binary hashes were rechecked. A fresh Git checkout normalizes 19 originally CRLF text inputs to LF under `.gitattributes`; their normalized contents and all production Git blobs are unchanged. Both byte identities are disclosed in `artifacts/v1.3-preparation/ci-followthrough-20260910.json`. Original package bytes and study evidence were not rewritten.

The correction and evidence were prepared in an isolated `codex/v1-3-ci-finalization` checkout. Concurrent edits to `site/index.html` and the untracked `site/assets/css/site-next.css` were preserved outside the uploaded candidate and are not covered by its website results. The final automated run and live PR checks should be read alongside the pending [host checklist](v1.3-host-checklist.md); an approval to upload does not constitute an observed host pass.
