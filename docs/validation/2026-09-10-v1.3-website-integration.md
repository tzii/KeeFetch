# Selected v1.3 website integration

The owner selected [PR #12](https://github.com/tzii/KeeFetch/pull/12), “Redesign KeeFetch's website with playful icon comparisons and cross-browser QA”, as the new website. Its exact head `d7a8ac9aff97052809a41590c113c4e5e34fa172` is integrated with the accepted release head `a93ed76eaf5382913567f61d541478495d15e491` for [PR #8](https://github.com/tzii/KeeFetch/pull/8). The only merge conflict was the current-state note; the accepted manual host results are preserved.

The selected warm-paper/violet design covers all seven pages, includes an explicitly labeled icon illustration, keeps the completed study's immutable evidence links, and adds Chromium, Firefox and WebKit CI. GitHub Pages remains the destination. The stable download stays v1.2.0 until v1.3 is actually published.

## Firefox failure and correction

The original PR12 run [34501945312](https://github.com/tzii/KeeFetch/actions/runs/34501945312) passed static checks, Chromium and WebKit, but Firefox was cancelled after 20 minutes. Its log stopped at the first JavaScript-disabled page. A bounded local reproduction found `document.fonts.status` already `loaded` while awaiting `document.fonts.ready` never settled in Firefox with JavaScript disabled. The same page could be captured successfully.

The runner now polls the synchronous font status from Python with a ten-second deadline. Font readiness, every JavaScript-disabled case and the existing visual assertions remain enforced. No production website change was needed for this harness defect.

## Integrated local validation

- Firefox 153.0, Playwright 1.62.0: **128/128 cases pass**, including seven pages at six widths in light/dark themes, scripts disabled, 200% text, keyboard operation, optional API/storage failures, forced colors, reduced motion, offline behavior, font fallback, print and long content.
- axe-core 4.10.3: **28 scans, zero reported violations**. Incomplete findings remain in the report; automated checks do not establish full accessibility conformance or screen-reader/physical-device coverage.
- **17/17 site verifier tests**; generated profile/release synchronization, seven-page source/build verification, JavaScript syntax, Python compilation and whitespace checks pass.
- Desktop light and mobile dark homepage captures were visually inspected at original resolution; no layout defect was found in those captures.

Local evidence is retained under `artifacts/v1.3-preparation/pr12-integrated-firefox-20260910/`, including `results.json` and screenshots. The bounded reproduction script and original cancelled-run screenshots are retained separately under the same preparation directory. The combined PR8 head must pass the full release build and the three-engine website workflow; the PR's checks provide the current remote result.

## Release identity and remaining media

The host-accepted package source remains `9f0b43f64335803d24ca99d6ccc7e0e7abac3bb8`. Rechecked retained hashes:

- DLL: `924c6c5c1a38b99e8805b01e20569f3fdb50c2941576c567f5e1f406e4311ad5`
- PLGX: `4742355b5cadb089c7898b99d7a6ff95d068f8319ad97b3a1eb15b1d186a53a8`

Neither package was rebuilt or replaced. The [owner-reported functional host acceptance](2026-09-10-v1.3-manual-host-results.md) remains valid; these changes affect the website, test harness and release records.

Four genuine synthetic-host screenshots remain pending: Settings Overview, Settings Providers, First Run and Completion. The supported native app tool again refused to launch KeePass with `product policy blocks this app`; a manual launch command was supplied to the owner. No alternate launch route was attempted and no Codex host observation is claimed. Add the genuine media when supplied, validate its integration, then complete release review and publication using the retained packages.
