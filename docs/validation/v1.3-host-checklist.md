# KeeFetch v1.3 — host checklist

Fill in PASS / FAIL beside each item and add a note or screenshot for failures.
This checklist uses a disposable KeePass copy and a synthetic database.

**Plugin source:** `9f0b43f64335803d24ca99d6ccc7e0e7abac3bb8`; final catalog, assembly/file version **1.3.0.0**.
**DLL SHA-256:** `924c6c5c1a38b99e8805b01e20569f3fdb50c2941576c567f5e1f406e4311ad5`.
**KeePass:** 2.60. **Database password:** `keefetch-test`.
**PLGX SHA-256:** `4742355b5cadb089c7898b99d7a6ff95d068f8319ad97b3a1eb15b1d186a53a8`.

## Open the test host

Run in PowerShell:

```powershell
$root = 'C:\Dev\KeeFetch\artifacts\v1.3-preparation\candidate-9f0b43f-20260910\portable-plgx'
& "$root\KeePass.exe" -cfg-local "$root\KeeFetch-v13-ui-regression.kdbx"
```

Enter `keefetch-test`. Use only this test database.

Date: __________  Windows version: __________  Display scale: __________

## 1. Load, first run and saving

- [ ] Plugin loads without a compilation/error dialog; Tools → KeeFetch exists.
- [ ] Download All Favicons offers the expected 71-entry confirmation.
- [ ] On the first-run profile screen, Cancel aborts before downloading.
- [ ] Start again, select Fast and confirm. After closing/reopening KeePass, Settings still shows Fast.
- [ ] In Settings, select Privacy and Cancel (or Escape). Reopen: Fast remains selected.
- [ ] Select Balanced and Save (or Enter). Reopen KeePass: Balanced remains selected.

Result / notes: ______________________________________________________

## 2. Keyboard only

Open Settings and put the mouse aside.

- [ ] Tab / Shift+Tab reach every control in a logical order; focus is visible.
- [ ] Ctrl+Tab switches Overview / Downloads / Providers / Advanced.
- [ ] Alt access keys activate the labelled controls; Space toggles checkboxes.
- [ ] Arrow keys move provider selection; provider controls work in Custom.
- [ ] Enter saves Settings; Escape cancels edits.
- [ ] On Completion, Enter closes safely. Copy Summary and Retry Eligible are keyboard reachable when available.

Result / notes: ______________________________________________________

## 3. Actual Windows display scaling

Change Windows display scaling, restarting the test KeePass when needed.
At each scale inspect Settings (all four tabs, minimum and enlarged sizes),
First Run, active Progress and Completion. Check long descriptions, buttons,
focus and diagnostics paths for overlap/clipping. Restore your usual scale afterward.

| Scale | PASS / FAIL | Affected form / notes |
|---|---|---|
| 100% | | |
| 125% | | |
| 150% | | |
| 200% | | |

## 4. Windows High Contrast

Enable a Windows contrast theme and repeat Settings, First Run, Progress and
Completion. Restore your normal theme afterward.

- [ ] Text, checked/unchecked controls, disabled controls and focus are distinguishable.
- [ ] Meaning is not conveyed only by color or an icon.
- [ ] No invisible labels, buttons, warnings or selected rows.

Result / notes: ______________________________________________________

## 5. Batch outcomes

Use the full synthetic database for mixed results. Live sites may change, so
record the observed counts rather than expecting a fixed success count.

- [ ] Cancel an active batch: observe Running → Cancelling → Cancelled.
- [ ] Completion counts add up: total = updated + skipped + not found + errors + cancelled. Invalid input is a subset of errors; do not add it twice.
- [ ] Cancelled entries are excluded from Retry Eligible.
- [ ] Run a mixed batch to completion. Copy Summary works; the listed log/CSV files exist.
- [ ] Retry Eligible processes only prior eligible misses/errors. The second Completion has no Retry action.
- [ ] Enable skipping existing icons and rerun an entry that now has an icon: it is counted as skipped.
- [ ] Select one or a few entries that successfully fetched before, disable skipping, and fetch them: a success-only completion reports no errors/misses/cancellations.
- [ ] In a copy of the test database with every entry in its Recycle Bin, Download All does not ask to download zero entries.

Cancelled summary: ___________________________________________________

Mixed summary: _______________________________________________________

Retry summary: _______________________________________________________

Skipped / full-success / Recycle Bin notes: ___________________________

## 6. Long diagnostics path

With KeePass closed, copy the synthetic database into a nested test directory
whose full path is roughly 180–220 characters. Open that copy and run a small batch.

- [ ] Completion remains usable with the long path; buttons are reachable.
- [ ] Copy Summary includes the complete path and the diagnostic files can be found.

Result / notes: ______________________________________________________

## 7. Legacy configuration migration

Close the disposable KeePass before editing its `KeePass.config.xml`. Keep a
backup of that file. Use a separate copy for each case. Remove `KeeFetch.*`
items from that copy first so an existing profile ID does not bypass migration.

Ready-made synthetic configurations are in
`docs/validation/fixtures/v1.3-legacy-configs/`. With the test host closed, copy
one into its portable directory as `KeePass.config.xml`. These files affect only
the disposable host and avoid editing a real v1.2 configuration.

The Custom fixture expects: Direct Site then Yandex then Google in the order
list; Yandex disabled, Google enabled; timeout 13 seconds; synthetic fallbacks
off; third-party fallbacks on; private/internal certificate-chain override on.
Inspect those settings before and after Save/restart. The checked certificate
option is intentional fixture data for this migration check.

If creating your own case instead, use this format:
Inside `<Configuration><Custom>`, add the legacy preset item below (create
`<Custom>` if absent):

```xml
<Item><Key>KeeFetch.FetchPresetMode</Key><Value>Fast</Value></Item>
```

Run each case twice; open Settings, check the result, Save, close and reopen.

| Legacy value | Expected profile | First launch | Second launch |
|---|---|---|---|
| Fast | Fast (`bulk-fast`) | | |
| Balanced | Balanced (`everyday`) | | |
| Thorough | Precise (`max-coverage`) | | |
| Custom | Custom (`custom`) | | |
| Turbo (unknown) | Custom (`custom`) | | |

For Custom, use the supplied synthetic fixture or seed the legacy provider
order/toggles, timeout, synthetic and certificate settings from a test v1.2 configuration.

- [ ] Custom provider order/toggles/timeouts/synthetic/certificate choices survive both launches.
- [ ] There is no unexpected first-run reset of a migrated configuration.

Custom values before / after: _________________________________________

## Send back

Return this file with results, or paste the failed item numbers and notes.
Do not include real database files or credentials. Screenshots should show
only the synthetic test host.

**Overall:** PASS / FAIL / NOT FINISHED

Outstanding items: ___________________________________________________

These results validate the exact identified v1.3 candidate. The expanded study and catalog freeze are complete. Do not replace these package bytes with a later rebuild while collecting evidence.

## 8. Exact formats, upgrade, rollback and screenshots

The candidate directory also contains `portable-dll` with the exact DLL, and `host-cases` with five separate migration hosts (`migration-fast`, `migration-balanced`, `migration-thorough`, `migration-custom`, `migration-turbo`). Each has its own synthetic database and local configuration. Close one KeePass test host before opening another. Use only these disposable copies.

- [ ] DLL host loads version 1.3.0.0; Tools → Plugins and Tools → KeeFetch show the expected plugin. PLGX host independently loads and compiles the identified PLGX.
- [ ] `host-cases/upgrade-rollback` starts with the actual published v1.2.0 PLGX, verified against its published checksum `1fd7e12590bfc2d23ede93c1ae5e61de0662187321caccdfeeb0bbcb995f5275`. Check the synthetic Custom settings in that old host and close it. Refresh `before-upgrade` backups if its settings/database changed, then replace only its Plugins/KeeFetch.plgx with the candidate's final PLGX. Restart and verify Custom settings, version and ordinary fetching.
- [ ] Close the upgrade host and restore the earlier plugin, configuration and database from `before-upgrade`. Restart and verify the old version, preserved Custom choices and the backed-up icon state. Restoring the plugin alone does not revert database changes.
- [ ] Use the prebuilt database under `host-cases/long-diagnostics-path-for-the-synthetic-database/nested-folder-with-another-deliberately-long-name` for the long-path check.
- [ ] Capture genuine final-host Settings Overview, Settings Providers, First Run and Completion screenshots for `site/assets/media/settings-overview.png`, `settings-providers.png`, `first-run.png`, and `completion-summary.png`. Keep other apps and real vaults out of the images.

Execution limitation: Codex's native computer-use runtime rejected launching this KeePass host with `product policy blocks this app`. No final visible host pass or final screenshot is claimed. The prepared tests require manual execution in the synthetic hosts.
