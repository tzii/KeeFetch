This is a focused re-review of three census units previously labeled ambiguous. Read machine-review/resolve-ambiguous/manifest.json - three images, each a cropped two-tone blue and white banner from the header of neverssl.com, a plain-HTTP connectivity test page, fetched by a favicon fetcher as the site icon. Your task: decide a definitive label for each unit - do not return ambiguous unless truly undecidable after following this procedure. Procedure: One - browse http://neverssl.com/ live and look at the page and its actual favicon if any; note what a correct icon for this site would be. Two - examine each image and its pixel stats in the manifest. Three - decide whether the cropped header banner can serve as the site favicon at all. Label guidance: a cropped fragment of page chrome that is not an icon and cannot serve as one is unusable - the label definition explicitly includes cropped beyond recognition or content that cannot serve as an icon. If the banner is actually the sites genuine favicon as served live, label correct. If it is a machine-generated plausible mark, acceptable-synthetic. If genuinely undecidable, keep ambiguous. Output ONLY a JSON array, one object per unit key: fields key, label, reason, verified_live. key copied verbatim from the manifest, label one of correct, acceptable-synthetic, generic, wrong-brand, blank, unusable, ambiguous, reason one short sentence, verified_live boolean.