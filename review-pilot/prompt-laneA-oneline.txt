Read review-pilot/manifest.json in this snapshot. It lists 20 favicon units; each has key, input_url, category, flags, and an image path relative to review-pilot. Open each image and label it with exactly one tag from this closed set: correct means clearly the genuine recognizable brand favicon of the site at input_url; acceptable-synthetic means a plausible generated or derived icon such as a letter mark from the domain that reasonably represents the site; generic means a valid image with generic content not specific to this site such as a globe or default CMS icon; wrong-brand means clearly a different brand or wrong site icon; blank means empty or fully transparent or no visible content; unusable means corrupt or unreadable or too degraded to judge; ambiguous means you cannot judge confidently. Judge brand correctness against your knowledge of the site at input_url and you may browse the live site to verify; record verified_live true or false per unit. Be conservative: if torn between two labels use ambiguous. Cross-check the flags field against what you see and mention contradictions in your reason. androidapp URLs are Android package ids, for example com.google.android.gm is Gmail, so correct means the right app icon. Deduplication and url-variant fixtures may legitimately share an icon with their canonical site. Answer with ONLY a JSON array of 20 objects, each with fields key, label, reason, verified_live - copy key strings verbatim from the manifest - and no other text before or after the array.